# 🔐 Secrets and configs
.streamlit/secrets.toml          # ✅ Prevents leaking Google credentials

# 🐍 Python internals
__pycache__/                     # ✅ Ignores Python bytecode cache
*.pyc                            # ✅ Prevents compiled junk from showing up

# 💻 macOS system files
.DS_Store                        # ✅ Avoids clutter from Finder

# 📄 Jupyter Notebook checkpoints (if used)
.ipynb_checkpoints/             # ✅ Keeps repo clean if any .ipynb files are used

# 🤫 Environment configs (if used)
.env
.env.*                           # ✅ Future-proof for `.env.dev`, `.env.local`, etc.

# ❌ Local virtual environment
survey_app/                      # ✅ Prevents pushing venv (confirmed from your screenshot)